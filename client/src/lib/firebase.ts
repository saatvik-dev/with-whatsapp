// This is a stub file to maintain import compatibility
// We're now using Netlify for deployment instead of Firebase

// Create dummy exports to avoid breaking existing imports
const app = null;
const analytics = null;

// Log a message to the console
console.log('Firebase not initialized: Using Netlify deployment instead');

// Export dummy values
export { app, analytics };
export default app;